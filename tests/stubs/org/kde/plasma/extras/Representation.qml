import QtQuick.Controls as QQC2

// Plasma's full-representation frame: a Page with header, contentItem and
// footer, which is all PopupView.qml asks of it.
QQC2.Page {
    property bool collapseMarginsHint: false
}
