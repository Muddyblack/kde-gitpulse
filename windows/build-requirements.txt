# Exact versions for the .exe that CI builds and releases ship, so a release
# rebuilt from its tag gets the same Qt, the same bootloader and the same
# hooks. The app's own needs are the range in windows/requirements.txt, which
# CI's test job installs as they come — a new PySide6 shows up there first.
# Keep these inside that range, and bump them on purpose (then try the build
# in a Windows VM; see docs/windows.md).
PySide6-Essentials==6.11.2
shiboken6==6.11.2
pyinstaller==6.22.2
pyinstaller-hooks-contrib==2026.7
altgraph==0.17.5
pefile==2024.8.26
pywin32-ctypes==0.2.3
setuptools==84.0.0
pillow==12.3.0
